const MyForm = {
    validate: () => ({
        isValid: 'Boolean',
        errorFields: 'String',
    }),

    getData: () => 'Object',

    setData: (Object) => undefined,

    submit: () => undefined,
}

document.addEventListener('DOMContentLoaded', () => {
    

}, false)
